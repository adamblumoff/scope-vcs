ok so i think we're in a decent arch spot right now, so i think the best thing is to add more features. Here's what i'm thinking, we need a repo-level config file that we can set to configure either public or private parts of our repo. this would probably be a json file (let me know if you think we should choose a different file name) that will have our repo configured just like in the web ui. changes to this file only apply on a "scope push". on every scope push we read the diff compared to the previously pushed repo to scope, ask if they want to review in the cli or the web ui (there should be flags for this to skip the prompt) and then there should be a tui on push that we can easily configure the permissions of each folder/file. there's probably a lot of stuff that i'm missing, but go ahead and tell me how you think we should implement this and even if we should implement this.