ok but the scope request start should go through auth and pull either private or public main based on the cli auth permission. every cli request should just attach auth header and then explicitly tell the user whether the action they are doing based on their level of permissioning. again i want all of the writes to be done via the cli and for the ui on the website to just be read only (just like repo view). the only thing that i'm worried about is doing a request via the cli might be a little tricky and bad ux, what do you think? otherwise i think your directions look good.