ok so next thing that's obvious is that we want review on push to be a tui, not just a list that we manually have to go through the json file and configure. so the tui should look very similar to the old visibility toggle web ui, where pressing space on a folder toggles between public and private, and pressing right arrow key on a folder shows the sub folders. this should be it's own command called "scope review" that runs as a part of "scope push" unless there is a flag --no-review on the push command. we should be able to call scope review at any time to change visibility for folder/file. please come up with an html plan for this that describes what you need to add/modify/delete to make this happen. also let me know if you have any questions