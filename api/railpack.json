{
  "$schema": "https://schema.railpack.com",
  "provider": "rust",
  "steps": {
    "build": {
      "commands": [
        "cargo build --release --locked && mkdir -p bin && cp target/release/api bin/scope-vcs"
      ]
    }
  },
  "deploy": {
    "aptPackages": ["git"],
    "startCommand": "./bin/scope-vcs"
  }
}
