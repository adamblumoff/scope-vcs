[package]
name = "scope-cache-contract"
version = "0.1.0"
edition = "2024"
license = "UNLICENSED"
repository = "https://github.com/adamblumoff/scope-vcs"

[dependencies]
scope_cache_domain = { package = "scope-cache-domain", path = "../scope-cache-domain" }
serde = { version = "1.0.228", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0.147"
