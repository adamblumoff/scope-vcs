{
  "$schema": "https://schema.railpack.com",
  "provider": "rust",
  "steps": {
    "build": {
      "commands": [
        "cargo build --release --locked -p worker && mkdir -p bin && cp target/release/worker bin/scope-worker"
      ]
    }
  },
  "deploy": {
    "aptPackages": ["git"],
    "startCommand": "./bin/scope-worker"
  }
}
