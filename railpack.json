{
  "$schema": "https://schema.railpack.com",
  "provider": "rust",
  "steps": {
    "build": {
      "commands": ["sh ./cli/scripts/build-railway.sh cli"]
    }
  },
  "deploy": {
    "startCommand": "SCOPE_CLI_ARTIFACT_DIR=./cli/dist ./cli/bin/scope-cli-service"
  }
}
