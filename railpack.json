{
  "$schema": "https://schema.railpack.com",
  "provider": "rust",
  "steps": {
    "build": {
      "commands": ["sh ./cli/scripts/build-railway.sh cli"]
    }
  },
  "deploy": {
    "startCommand": "./cli/bin/scope-cli-service"
  }
}
