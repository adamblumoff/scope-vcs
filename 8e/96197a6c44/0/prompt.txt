perfect, that's actually exactly what we wanted, but we autotmatically should put it in the .gitignore if it isn't already in there. go ahead and make that change on main and do an autoreview for that. since we're only pushing off commits going forward, we don't need commands to change prior git history, only the last commit.