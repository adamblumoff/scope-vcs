{
  "$schema": "https://schema.railpack.com",
  "provider": "rust",
  "steps": {
    "build": {
      "commands": [
        "cargo build --release --locked -p scope-repo-router && mkdir -p bin && cp target/release/scope-repo-router bin/scope-repo-router"
      ]
    }
  }
}
